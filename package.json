{
  "name": "luntia",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "build": "turbo build",
    "dev": "docker-compose up -d postgres && turbo dev",
    "dev:full": "docker-compose up",
    "dev:down": "docker-compose down",
    "setup-dev-db": "docker-compose up -d postgres",
    "lint": "turbo lint",
    "format": "turbo format",
    "check": "turbo check",
    "test": "turbo test",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.3",
    "turbo": "^2.5.8"
  },
  "lint-staged": {
    "server/src/**/*.{ts,js}": [
      "biome check --write --no-errors-on-unmatched"
    ],
    "client/src/**/*.{ts,tsx,js,jsx}": [
      "biome check --write --no-errors-on-unmatched"
    ]
  }
}
